<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <link rel="stylesheet" href="main.css">
    <title></title>
  </head>
  <body>
      <!-- content should appear here. -->
      <main class=userMain>

        <img src="{{avatar}}">
        <h1>{{name}}</h1>
        <p>
          {{#address.city}}
            {{address.city}},
          {{/address.city}}
          {{#address.country}}
           {{address.country}} |
          {{/address.country}}
          {{#job}}
           {{job}}, {{company}}
          {{/job}}
          {{^job}}
            <span class="unemp">
              Available for hire
            </span>
          {{/job}}
         </p>
      </main>

  </body>
</html>
